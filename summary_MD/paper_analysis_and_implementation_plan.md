# 论文分析与 ego-planner-swarm 移植方案

## 1. 论文核心思想分析 (AICG)

**论文标题**: 基于覆盖的自适应拦截区域协同制导律 (Adaptive Interception Zone Cooperative Guidance, AICG)

### 1.1 核心问题
解决多枚拦截器（在本项目中对应“多架无人机”）如何协同围捕一个高机动目标（Target）。主要挑战在于目标机动未知，且需要保证没有任何“逃逸角”能让目标逃脱。

### 1.2 关键算法原理
1.  **阿波罗尼奥斯圆 (Apollonius Circle)**:
    -   利用拦截器与目标的速度比 $k = V_M / V_T$，构建描述“同时间到达”的时空遭遇几何边界。
    -   由此推导出目标的**最大逃逸角范围** $U_{	heta, T}$（论文公式 12）。这是一个角度区间，目标只能在这个角度范围内尝试逃逸。

2.  **自适应区域分配 (Adaptive Coverage)**:
    -   **思想**: 不预先给每个无人机分配固定的包围扇区，而是根据实时的运动状态动态调整。
    -   **方法**: 每个无人机只与**左右邻居**通信。通过平衡自己与左右邻居的“角度间隙” ($h_{i, i+1}$)，计算出自己当前的**期望视线角** ($q_{d,i}$)。
    -   **公式**: $q_{d,i}(	au)$ 的更新旨在让所有无人机在角度上均匀分布，封锁目标的逃逸范围（论文公式 15, 16）。

3.  **制导控制**:
    -   论文设计了非线性干扰观测器 (NDO) 估计目标加速度。
    -   设计了固定时间收敛的滑模控制律，使无人机追踪期望视线角 $q_{d,i}$。

---

## 2. 系统架构适配 (ego-planner-swarm)

在 `ego-planner-swarm` 项目中，底层轨迹规划和避障已经非常成熟。我们不需要用论文的滑模控制律直接控制电机，而是应该将 **AICG 算法作为上层决策模块 (Planner/Coordinator)**。

**架构设计**:
*   **输入**: 目标状态 (位置、速度)、群组内各机状态。
*   **AICG Coordinator (核心新模块)**: 运行论文的“自适应分配策略”，计算每架无人机的**期望位置**。
*   **EGO-Planner (现有)**: 接收期望位置作为 Goal，规划出无碰撞、平滑的轨迹。

### 角色映射
| 论文概念 | 项目映射 | 备注 | 
| :--- | :--- | :--- |
| **拦截器 (Interceptor)** | **Swarm Drones** | 执行包围任务的无人机群 |
| **目标 (Target)** | **Dynamic Object** | `obj_generator` 生成的球体或另一架无人机 |
| **期望视线角 ($q_{d,i}$)** | **Goal Position** | 需要将角度转换为围绕目标的 Cartesian 坐标点 |
| **SMC 控制律 ($a_m$)** | **EGO-Planner Trajectory** | EGO-Planner 负责实现到底层的加速度控制 |

---

## 3. 具体实施计划 (Action Plan)

### 第一步：创建 AICG 协同计算节点
建议新建一个 C++ 节点（或 Python 脚本），命名为 `aicg_coordinator`。

**功能**:
1.  **订阅 (Subscribe)**:
    -   `/drone_0/odometry`, `/drone_1/odometry`, ... (群组位置)
    -   `/obj_generator/odometry` (目标位置，或来自视觉检测)
2.  **计算 (Update Loop)**:
    -   **排序**: 根据相对于目标的当前角度，对 Swarm ID 进行逻辑排序（确定谁是谁的左右邻居）。
    -   **阿氏圆计算**: 实现论文 Eq(6-8)，计算 $k$ 值及阿氏圆参数。
    -   **逃逸区计算**: 实现 Eq(12)，得到 $U_{	heta, T}$。
    -   **期望角更新**: 实现 Eq(16)，计算每个无人机的 $q_{d,i}$。
    -   **目标点生成**:
        -   论文是“拦截(撞击)”，我们改为“包围”。
        -   $Goal_i = P_{Target} + R_{safe} 
cdot [
cos(q_{d,i}), 
sin(q_{d,i})]^T$
        -   其中 $R_{safe}$ 是安全包围半径。
3.  **发布 (Publish)**:
    -   `/drone_i/move_base_simple/goal` (发送给 EGO-Planner)

### 第二步：核心算法实现细节 (伪代码)

你需要实现的核心函数如下：

```cpp
// 1. 计算逃逸角范围 (论文 Eq 10, 11, 12)
// Input: 目标速度 Vt, 自身速度 Vm, 相对位置
// Output: [theta_min, theta_max]
pair<double, double> calculateEscapeZone(State target, State self, double a_T_max) {
    double k = target.vel.norm() / self.vel.norm(); // 速度比
    // ... 实现阿氏圆逻辑 ...
    // 返回逃逸角区间
}

// 2. 计算邻居间隙 (论文 Eq 15)
// q_i: 当前视线角, q_next: 邻居视线角, U_theta: 逃逸区
double calculateGap(double q_i, double q_next, Interval U_theta) {
    // 逻辑：计算两个角之间的跨度，并考虑逃逸区覆盖情况
    // return h_{i, i+1}
}

// 3. 更新期望角度 (论文 Eq 16)
double updateDesiredAngle(double q_i, double h_left, double h_right) {
    return q_i + (h_right - h_left) / 2.0;
}
```

### 第三步：集成与仿真
1.  **场景搭建**:
    -   使用 `run_hunting_demo.sh` 作为模板。
    -   修改 launch 文件，加入 `aicg_coordinator` 节点。
    -   确保 `obj_generator` 产生一个做机动运动的目标（例如正弦波或圆形轨迹）。
2.  **测试验证**:
    -   观察 Rviz。
    -   验证无人机是否均匀分布在目标周围。
    -   当目标加速转向时，无人机群是否会动态调整位置以封锁前方路径。

## 4. 潜在问题与应对
1.  **3D 空间扩展**: 论文基于 2D 平面。
    -   *简化方案*: 仅在 XY 平面执行 AICG 算法，Z 轴高度保持一致或跟随目标高度。
2.  **通讯延迟**:
    -   EGO-Planner 的 Swarm Bridge 已经处理了部分通信。如果延迟导致震荡，需在 $q_{d,i}$ 更新中加入低通滤波。
3.  **碰撞避免**:
    -   AICG 只管角度分配，可能会导致无人机相互太近。
    -   *依靠*: EGO-Planner 底层的 `Rebound` 优化器会处理机间避障。只要 Goal 点不要重合在一点即可（AICG 算法本身就是分散角度的，天然有助于防碰撞）。

## 5. 总结
你需要做的工作主要是 **编写 `aicg_coordinator` 节点**，将论文的数学公式转化为代码，并将计算出的“期望角度”转化为 `ego-planner` 能听懂的“目标点坐标”。无需修改底层的 B-spline 优化器。
	

### 一、 当前仿真实现 vs. 论文理论算法 (Algorithm Gaps)

目前的代码 (`aicg_coordinator.py`) 实现了一个**高层策略适配器**，而非论文中底层的**制导控制律**。具体差距如下：

#### 1. 控制架构不同 (Guidance vs. Planning)
*   **论文方法 (ISMC)**：论文设计的是一种**制导律 (Guidance Law)**，直接输出**加速度指令 ($a_m$)**。它使用了固定时间收敛积分滑模控制 (Fixed-time ISMC) 来直接驱动导弹/无人机消除角度误差。
*   **当前实现 (High-level Coordination)**：采用了**分层架构**。`aicg_coordinator` 仅计算期望的**位置点 (Waypoints/Goals)**，然后交由 `ego-planner` 进行轨迹规划。
    *   **差距**：响应速度和动态特性不同。论文的方法是瞬时响应的底层的控制，能够处理极高动态的对抗；而当前实现依赖 `ego-planner` 的重规划（Re-planning），存在规划耗时（几十毫秒到几百毫秒），面对高频机动目标时会有滞后。

#### 2. 目标状态获取与估计 (NDO 缺失)
*   **论文方法**：设计了**非线性干扰观测器 (NDO)** 来估计目标的加速度 ($a_T$)，视其为扰动并进行补偿。这是实现“大机动目标”拦截的关键。
*   **当前实现**：通过 ROS 话题直接订阅目标的**真值里程计 (`odom`)** 获取速度信息，且未利用加速度信息。
    *   **差距**：当前系统假设拥有“上帝视角”的完美目标状态信息，缺乏对目标未知机动的鲁棒估计能力。

#### 3. 通信与计算架构 (Centralized vs. Distributed)
*   **论文方法**：明确提出了**分布式**架构，假设无人机只与左右邻居通信 (Ring Topology)，利用局部信息更新自己的期望角度。
*   **当前实现**：`aicg_coordinator.py` 是一个**集中式**节点，它订阅了所有飞机的 `odom`，计算完所有人的目标点后统一分发。
    *   **差距**：虽然算法逻辑上模拟了“只看邻居”的权重计算，但在工程实现上仍然是单点式的。如果中心节点挂掉，整个集群瘫痪。

#### 4. 碰撞避免处理
*   **论文方法**：主要关注角度分配，通常假设在同一平面但在不同半径或通过高度差/势场函数处理避障（论文中未详细展开机间避障的动力学约束）。
*   **当前实现**：完全依赖 `ego-planner` 的 `swarm_clearance` 参数和 B-spline 优化来避免碰撞。
    *   **差距**：这是一个**优势**。当前实现利用 `ego-planner` 强大的避障能力，比论文纯制导律更容易在复杂障碍物环境中生存。但如果围捕半径过小，`ego-planner` 可能会因为解不出无碰撞轨迹而报错（如之前的 Solver Error）。

---

### 二、 仿真环境 vs. 实机部署差距 (Deployment Gaps)

要将目前的仿真代码部署到真实无人机集群上，存在显著的工程鸿沟：

#### 1. 感知系统 (Perception is Missing)
*   **仿真现状**：拦截机通过 ROS 话题直接“读取”目标机的坐标 (Ground Truth)。
*   **实机挑战**：拦截机**不知道**目标机在哪里。
    *   **必须补充**：需要机载视觉（如 YOLO, SiamRPN）或雷达来实时检测并跟踪目标，输出目标相对于拦截机的位置和速度。
    *   **协同感知**：单机视野受限，可能需要多机通过通信融合目标位置（Target Re-identification & Fusion）。

#### 2. 通信系统 (Communication Links)
*   **仿真现状**：ROS 内部通信，零延迟，无限带宽，自动发现。
*   **实机挑战**：
    *   需部署实际的通信模块（Wifi 广播, ZigBee, 或数传电台）。
    *   需解决**通信延迟**和**丢包**问题。AICG 算法对邻居状态的实时性要求较高，延迟会导致围捕队形震荡。
    *   代码需要从“读取所有 topic”改为“通过网络接收邻居数据包”。

#### 3. 状态估计 (State Estimation)
*   **仿真现状**：使用 `uav_simulator` 提供的完美里程计。
*   **实机挑战**：
    *   实机依赖 VIO (Visual-Inertial Odometry) 或 GPS。
    *   **漂移问题**：如果使用 VIO，三架拦截机的坐标系原点不同，需要进行**地图/坐标系校准 (Map Merging / Aligning)**。否则 Drone 1 认为的 (0,0) 和 Drone 2 认为的 (0,0) 不是同一个点，导致围捕失败甚至相撞。

#### 4. 动力学限制
*   **仿真现状**：`poscmd_2_odom` 模拟器是简化的动力学模型，且我们大幅降低了控制频率（2Hz）和增益。
*   **实机挑战**：
    *   真实无人机存在电机响应延迟、电池电压下降导致的推力减弱、风阻干扰等。
    *   `ego-planner` 生成的轨迹如果过于激进，实机可能会跟不上（Tracking Error），导致碰撞。

#### 5. 初始条件与启动流程
*   **仿真现状**：一键脚本在固定位置生成所有飞机。
*   **实机挑战**：
    *   需要通过遥控器或地面站逐个解锁、起飞。
    *   需要一个**有限状态机 (FSM)** 来管理任务流程：`待机 -> 起飞 -> 搜索目标 -> AICG围捕 -> 任务结束/返航`。目前的脚本一上来就直接进入围捕计算。

### 三、 总结与建议

**目前状态**：
您已经成功验证了 **AICG 的高层策略逻辑**（即基于逃逸区权重的角度动态分配）。在 `ego-planner` 的辅助下，实现了无碰撞的围捕运动规划。这作为验证算法有效性的原型（Prototype）是非常成功的。

**下一步改进建议**：
1.  **引入观测器**：尝试在 Python 脚本中加入简单的 Kalman 滤波或 NDO，仅使用带噪声的位置数据来估计目标速度，模拟真实感知。
2.  **去中心化改造**：将 `aicg_coordinator.py` 拆分为 `aicg_node.py`，每架无人机运行一个实例，只订阅“邻居”和“目标”的话题，验证分布式逻辑。
3.  **感知模拟**：在仿真中给 `TargetMover` 的位置数据加入高斯噪声，测试算法鲁棒性。